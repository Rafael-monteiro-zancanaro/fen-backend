package org.fen.fen.domain;

import org.junit.jupiter.api.Test;

class EntidadesBasicasAuditoriaTest {

    @Test
    void entidadesBasicasSaoAuditadas() {
//        assertThat(Paciente.class).hasAnnotation(Audited.class);
//        assertThat(Endereco.class).hasAnnotation(Audited.class);
//        assertThat(Estagiario.class).hasAnnotation(Audited.class);
//        assertThat(Medicamento.class).hasAnnotation(Audited.class);
//        assertThat(FormaFarmaceutica.class).hasAnnotation(Audited.class);
//        assertThat(ClasseTerapeutica.class).hasAnnotation(Audited.class);
//        assertThat(Comorbidade.class).hasAnnotation(Audited.class);
//        assertThat(Interacao.class).hasAnnotation(Audited.class);
    }
}
